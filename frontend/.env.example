# FastAPI 백엔드 base URL (서버 컴포넌트·proxy가 호출). 기본값이 로컬 개발용.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# 구글 OAuth 클라이언트 ID (공개값) — consent URL 생성에 사용.
# backend/.env 의 GOOGLE_CLIENT_ID 와 같은 값. client_secret은 백엔드에만 둔다.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=